# Utilark 확인·결정 대기 목록

AdSense 준비 작업(`6e62b57`)에서 남은 항목입니다. 코드로 끝낼 수 없거나 결정이 필요한 것만 적습니다.

경쟁 키워드 조사는 `docs/seo-research.md`에, 서브도메인 정책은 `docs/subdomains.md`에, 구글 검색 동작 방식은 `docs/how-search-works.md`에 있습니다.

## 사람이 직접 해야 하는 것

- [x] ~~**Google Search Console 등록**~~ — 2026-08-13 완료. 사이트맵 제출과 색인 생성 요청이 남았다면 아래 2·3단계를 마저 하세요. 왜 이 단계가 필요한지는 `docs/how-search-works.md`에 정리했습니다 — 구글은 이미 크롤링한 페이지의 링크를 따라 새 URL을 찾으므로, 아무도 링크를 걸어주지 않은 새 도메인은 직접 알리는 것 말고는 발견될 경로가 없습니다.

- [ ] **사이트맵 제출 + 색인 생성 요청** — 속성 등록만으로는 색인되지 않습니다. Search Console에서 두 가지를 더 해야 합니다.

  1. **Sitemaps** → `sitemap-index.xml` 제출.
  2. **URL 검사**에 `https://utilark.app/ko/`를 넣고 **색인 생성 요청**. 주요 도구 페이지도 같이. 크롤러를 기다리는 것보다 빠릅니다.

  빌드 쪽은 막는 게 없습니다 — `robots.txt`가 `Allow: /`, 사이트맵에 44개 URL(도구·변환 쌍·가이드 포함)이 전부 새 주소로 들어 있고, 홈·도구 페이지에 `noindex`가 없습니다(`npm test`가 확인). URL을 2026-08-13에 `/{언어}/{슬러그}/`로 바꿨으므로 지금 제출하면 처음부터 최종 주소로 들어갑니다.

  제출해도 즉시 나오지는 않습니다. 원문이 "크롤링·색인·게재를 보장하지 않는다"고 못 박고 있고, 보통 며칠에서 몇 주가 걸립니다(`docs/how-search-works.md`).

- [ ] **네이버 서치어드바이저 등록** — 한국어 시장에 집중하기로 했으므로(아래 "이름 유지 결정") 네이버는 구글과 별개로 챙겨야 합니다. 네이버는 자체 색인을 쓰기 때문에 Search Console 등록이 네이버 노출에 아무 영향을 주지 않습니다. `글자수 세기`·`PDF 합치기`·`사다리타기` 같은 조사한 키워드는 국내 검색 비중이 큽니다.

  구글과 전제가 다릅니다 — 네이버는 **"검색 등록 절차는 존재하지 않습니다"**라고 명시하고, 등록하지 않아도 수집 로봇이 자동으로 찾아 반영합니다. 등록의 값은 ① 로봇에게 존재를 알리는 가장 쉬운 방법이라는 것과 ② 수집·색인·노출 리포트를 볼 수 있다는 것이고, 리포트는 소유확인을 마쳐야 열립니다.

  **메타태그 삽입은 코드에 끝나 있습니다.** 값만 넣으면 됩니다.

  1. [searchadvisor.naver.com](https://searchadvisor.naver.com) → 사이트 추가 `https://utilark.app` → 소유확인 **HTML 태그** 선택 → `content="..."` 안의 값만 복사(태그 전체 아님).
  2. Settings → Secrets and variables → Actions → **Variables** 탭에 `PUBLIC_NAVER_SITE_VERIFICATION` 추가. Secrets 탭이 아닙니다.
  3. 재배포(빌드 시점에 읽습니다) 후 네이버에서 **소유확인**.
  4. 확인되면 **요청 → 사이트맵 제출**에 `sitemap-index.xml`.

  **소유확인 제약 네 가지를 전부 확인했고 통과합니다.** 서치어드바이저 도움말 기준입니다.

  | 제약 | 이 사이트 |
  |---|---|
  | 브라우저단 리다이렉션(JS·meta refresh·cookie) 불가, **HTTP 301·302는 지원** | 루트 `/`는 Worker가 내는 서버측 302 |
  | 자바스크립트 실행 미지원 | Astro static이라 태그가 빌드 산출 HTML에 그대로 있음 |
  | `<body>` 안이면 검증 제외 | `<head>` 안 |
  | frame 태그 안이면 검증 제외 | frame 없음 |

  `npm test`가 head 배치와 frame 부재를 검사하므로 나중에 조용히 깨지지 않습니다.

  2016년 블로그 글("소유확인이 안돼요 - 사이트 리디렉션")은 301만 보장한다고 하지만, 현재 도움말은 301·302를 함께 지원한다고 명시합니다. 도움말이 최신입니다.

  **실패하면 HTML 파일 방식**으로 가세요. 루트를 아예 안 거칩니다. 네이버가 주는 `naverXXXX.html`을 `public/`에 넣고 커밋·배포하면 끝이고, 파일을 주시면 제가 넣어드리겠습니다. Worker 쪽은 준비돼 있습니다 — assets 라우팅의 `force-trailing-slash`가 `.html` 경로를 리다이렉트로 바꾸는 것을 막아 원래 주소에서 200으로 응답합니다.

- [ ] **AdSense 저장소 변수 등록** — Settings → Secrets and variables → Actions → **Variables** 탭에 `PUBLIC_ADSENSE_CLIENT` 추가. Secrets 탭이 아닙니다. 등록 후 재배포해야 심사용 스크립트와 `/ads.txt`가 살아납니다.
- [ ] **AdSense 콘솔에서 동의 메시지 게시** — 개인정보 보호 및 메시지에서 GDPR 메시지와 미국 주법 메시지를 게시. Google 인증 동의 메시지는 `adsbygoogle.js`를 통해 전달되므로 저장소에 추가할 스크립트는 없습니다. 이 단계를 건너뛰면 EEA·영국·스위스 게재가 규정 위반입니다.
- [ ] **승인 후 `PUBLIC_ADSENSE_SLOT` 등록** — 도구 페이지에 실제 광고 단위가 렌더됩니다.

## 검증하지 못한 것

- [ ] **라이브 루트 302 동작 확인** — 개발 세션의 네트워크 정책이 `utilark.app` 접근을 차단해(egress 403) 배포된 엣지 동작을 확인하지 못했습니다. 빌드 산출물과 Worker 단위 테스트는 통과했지만, `run_worker_first`와 assets 라우팅의 실제 상호작용은 런타임에서만 드러납니다.

  ```bash
  curl -sI -H 'Accept-Language: ko-KR,ko;q=0.9' https://utilark.app/ | head -3   # 302 → /ko/
  curl -sI -H 'Accept-Language: fr-FR' https://utilark.app/ | head -3            # 302 → /en/
  curl -sI -H 'Cookie: utilark_lang=ko' https://utilark.app/ | head -3           # 302 → /ko/
  curl -s https://utilark.app/ads.txt                                            # 변수 미설정 시 주석 한 줄
  ```

  루트가 여전히 언어 선택 HTML을 반환하면 `wrangler.jsonc`의 assets 라우팅을 다시 봐야 합니다.

- [ ] **라이브 서브도메인 리다이렉트 확인** — 같은 egress 차단으로 확인하지 못했습니다. DNS는 세션에서 조회해 확인했고(`mergepdf`·`png2jpg`·`pdf2image`가 모두 Cloudflare로 해석), 라우트는 배포 로그에 찍혔습니다(`*.utilark.app/* (zone name: utilark.app)`). 남은 건 실제 응답입니다.

  ```bash
  curl -sI -H 'Accept-Language: ko-KR,ko;q=0.9' https://mergepdf.utilark.app/ | head -5  # 302 → utilark.app/ko/merge-pdf/
  curl -sI -H 'Accept-Language: en-US' https://png2jpg.utilark.app/ | head -5            # 302 → utilark.app/en/png-to-jpg/
  curl -sI https://mergepdf.utilark.app/ko/privacy/ | head -5                            # 301 → utilark.app/ko/privacy/
  curl -sI https://www.utilark.app/ | head -5                                            # 301 → utilark.app/
  curl -sI https://utilark.app/ko/tools/merge-pdf/ | head -5                             # 301 → /ko/merge-pdf/ (옛 경로)
  ```

  `1001`·`1016` 오류가 나오면 와일드카드 DNS 레코드가 프록시(주황 구름)가 아닌 DNS only(회색)인지 확인해야 합니다. 회색이면 라우트가 걸리지 않습니다.

- [ ] **사진 회전(EXIF) 실기기 확인** — 이미지 변환기가 `createImageBitmap(file)`을 옵션 없이 부르고 있었습니다. 폰 사진은 가로 픽셀 + EXIF 회전 태그로 저장되는데, 그 태그를 적용할지는 브라우저 기본값에 달려 있고 버전마다 다릅니다. `{ imageOrientation: 'from-image' }`를 명시하도록 고쳤지만(이미 기본이면 무해, 아니면 버그 수정), **폰으로 세로 사진을 찍어 변환했을 때 눕지 않는지는 실기기에서만 확인됩니다.** 사진 관련 기능을 붙이기 전에 확인하는 게 좋습니다.

- [ ] **사진 자르기 실기기 확인** — 로직과 타입 검사만 통과한 상태이고, 포인터 조작은 실기기에서만 드러납니다. 어둡게 처리를 네 장의 패널로 바꿨으니 **선택 영역 바깥만 어두워지고 아래 버튼은 멀쩡한지** 먼저 봐주세요(이전에는 한 장의 큰 그림자가 프레임을 넘어 번졌습니다). **자유 비율에서 정사각·4:3·16:9·증명사진·원본 비율 근처로 끌면 딱 붙는지**(허용 오차 2.5%, 붙으면 테두리가 노랗게 변하고 선택 영역 오른쪽 위 표시에 `3:4 (877 × 1170)`처럼 비율 이름이 나옵니다), 반대로 아무 비율에나 계속 붙어서 거슬리지는 않는지도 봐주세요. **A4는 선택 영역의 짧은 변이 1240픽셀(210mm를 150dpi로) 이상일 때만 후보에 듭니다** — 작은 사진에서 A4가 안 뜨는 것이 정상이고, 큰 사진에서도 안 뜨면 그게 문제입니다. 치수 표시가 **선택 영역 안**에 있어 읽히는지(영역이 낮거나 좁으면 상자 위로 올라갑니다), 체크박스로 껐다 켜지는지도 봐주세요. 그 밖에 확인할 것: 손가락으로 모서리 손잡이를 끌 때 잡히는지(터치 대상 1.5rem), 화면 가장자리를 넘어가도 선택 영역이 가장자리에 붙어 멈추는지, 비율을 고정한 채 대각선으로 끌 때 모양이 유지되는지, 화면을 회전했을 때 선택 영역이 유지되는지(좌표를 원본 픽셀로 들고 있어 유지돼야 함), 그리고 **세로로 찍은 사진이 눕지 않는지**(위 EXIF 항목과 같은 원인).

- [ ] **사진 PDF 만들기 실기기 확인** — 확인할 것: 휴대폰에서 파일 선택이 **카메라를 띄우는지**(그래서 `accept="image/*"`로 뒀습니다), 세로로 찍은 페이지가 눕지 않는지, 여러 장을 넣었을 때 순서가 맞는지, A4 옵션에서 가로 사진이 가로 페이지로 나오는지, 그리고 **결과 PDF 용량이 메일로 보낼 만한지**(기본값 긴 변 1600px). 아이폰 HEIC 사진이 들어올 때 어떻게 되는지도 봐주세요 — 브라우저가 JPEG로 바꿔 주는 게 보통이지만 아니면 실패 메시지가 뜹니다.

- [ ] **텍스트 읽어주기 실기기 확인** — `speechSynthesis`는 브라우저마다 함정이 많고 대부분 실기기에서만 드러납니다. 확인할 것: **iOS에서 첫 재생이 되는지**(사용자 제스처 안에서 `speak()`를 부르도록 `await` 없이 짰습니다), 긴 글이 도중에 끊기지 않는지(문장 단위로 나눠 큐로 잇습니다), 목소리 목록이 뜨는지(`getVoices()` 첫 호출이 비는 브라우저 대비로 `voiceschanged`를 1초까지 기다립니다), 페이지를 가리면 소리가 멈추는지, 그리고 **한국어 목소리가 있는 기기에서 기기 내장 목소리가 먼저 뜨는지**.

  근거는 Bubblelab `_shared/tts.js`입니다. 같은 함정 목록을 문서화해 둔 파일이라 그대로 따랐습니다.

  플레이어도 함께 확인해주세요 — 문장 목록에서 한 줄을 누르면 거기서부터 읽는지, ◀◀·▶▶로 문장을 넘나드는지, 진행 막대가 따라오는지, 현재 문장이 자동으로 보이게 스크롤되는지, 그리고 **다른 탭으로 옮겨도 계속 읽는지**(의도된 동작입니다. 페이지를 벗어나면 멈춥니다).

- [ ] **유효 방문자 비율 확인 (배포 후 며칠 뒤)** — 관리자 화면의 `오늘 유효 비율`을 보세요. 집계는 배포 시점부터 시작하고 소급하지 않습니다. 비율이 지나치게 낮으면 크롤러 유입이 많다는 뜻이고, 반대로 전체 DAU와 거의 같으면 3초 기준이 느슨한지 의심해볼 수 있습니다. **공개 푸터 숫자를 유효 방문자로 바꿀지도 이 값을 보고 정하시면 됩니다** — 공개 API가 이미 두 값을 내려주므로 표시만 바꾸면 됩니다.

- [ ] **사다리타기 실기기 확인** — 빌드와 타입 검사만 통과한 상태입니다. 모바일에서 사다리 SVG의 세로줄이 위아래 이름칸 가운데와 정확히 맞는지, 화면을 회전했을 때 좌표가 다시 잡히는지 확인이 필요합니다.

## 보류 중인 기능

- [ ] **단어카드 (`claude/flashcards-tool` 브랜치)** — 구현은 끝났고 타입 검사와 빌드를 통과했지만 main에는 올리지 않았습니다. 앞면을 보여주고 제한 시간(기본 3초) 안에 뒷면을 타이핑하거나 말해서 맞히는 도구로, 맞힌 카드는 대기열에서 빼고 틀린 카드는 모아 섞어서 다음 회차에 다시 냅니다. 영문 페이지 본문 571단어로 기존 도구(235단어)보다 콘텐츠도 두껍습니다.

  올리기 전에 정해야 할 것:
  - **음성 인식의 개인정보 문제** — 이것이 가장 큰 쟁점입니다. `SpeechRecognition`은 브라우저 기능이고 Chrome·Safari 모두 녹음한 음성을 제조사 서버로 보내 글자로 바꿉니다. "파일이 기기를 떠나지 않는다"는 사이트의 핵심 주장과 정면으로 부딪히므로, 기본값 끄기 + 켤 때 경고 + 처리방침 문단 추가로 설계해 두었습니다(브랜치에 포함). 그래도 광고 심사를 앞두고 이 예외를 만들지, 아니면 음성을 빼고 타이핑 전용으로 낼지 결정이 필요합니다.
  - **3초가 실제로 쓸 만한지** — 짧은 영단어 기준으로 잡았고 5초·10초 선택지를 넣어 두었지만 실기기 확인이 필요합니다.
  - 그 외 실기기 확인: Firefox는 음성 인식 미지원이라 설정이 자동으로 비활성화됩니다.

## 결정이 필요한 것

- [ ] **광고 문구 시점** — 개인정보 처리방침의 광고 문단을 현재형("Utilark는 Google AdSense가 제공하는 광고를 게재합니다")으로 써 두었습니다. `PUBLIC_ADSENSE_CLIENT`를 설정해 배포하는 시점부터 사실이 됩니다. 당분간 변수를 설정하지 않을 계획이면 문구가 실제보다 앞서므로 되돌려야 합니다.
- [ ] **원격 브랜치 정리** — `claude/google-adsense-eligibility-b6ke9x`는 main에 모두 머지됐지만 원격에 남아 있습니다.
- [ ] **광고 노출 시 실제 배치 확인** — `PUBLIC_ADSENSE_SLOT`을 넣기 전에는 슬롯이 렌더되지 않아 실기기 확인이 불가능합니다. 넣은 뒤 모바일에서 **결과 다운로드 버튼과 광고 사이 간격**을 눈으로 보세요. FoxyUtils는 도구보다 위에 광고를 두 개 깔아 도구가 한참 밀리는데(`docs/seo-research.md`의 추가 조사), Utilark는 도구 아래 `3.5rem`으로 떨어뜨려 뒀습니다. 오터치가 광고 클릭이 되면 AdSense 정책 위반 위험도 있습니다.

- [ ] **`jpg-to-pdf`·`png-to-pdf` 쌍 페이지 여부** — 이미지↔이미지처럼 쌍으로 나눌지 결정이 필요합니다. 이쪽은 방향별로 할 이야기가 거의 같아서(둘 다 "사진을 PDF에 넣는다") 나누면 **중복 클러스터링 위험**이 이미지 쌍보다 큽니다. 지금은 `image-to-pdf` 한 페이지가 제목으로 `사진 PDF 변환`과 `JPG PDF 만들기`를 함께 담고 있습니다. 색인 결과를 보고 판단하는 편이 낫습니다.

- [ ] **가이드 3편 더** — `/{lang}/guides/`를 신설하고 3편을 썼습니다(아래 "가이드 섹션 신설"). 사다리타기에 붙는 가이드가 아직 없고, 이미지 변환은 쌍별 페이지로 나눌 때 쌍마다 짧은 가이드를 붙이는 편이 좋습니다. 도구 페이지 본문(235단어)을 억지로 늘리기보다 지면을 나누는 쪽이 관찰된 패턴에 맞습니다(`docs/seo-research.md`의 FoxyUtils 조사).
- [ ] **남은 Bubblelab 마이그레이션 후보** — `calendar`, `photo`, `passport-pic`, `stars`가 브라우저 전용으로 남아 있습니다. `passport-pic`(증명사진)이 검색 수요와 콘텐츠 밀도 면에서 다음 후보로 가장 좋아 보입니다. `brief`·`fortune`·`planner`·`chat`은 서버가 필요해 제외했고, `proofread`는 한국어 전용이라 별도 판단이 필요합니다.

## 정리된 것

- **랜딩 검색 + 모바일 2열 (2026-08-14)** — 도구가 7개, 변환 쌍이 6개, 가이드가 3편이 되면서 랜딩이 한 화면에 안 들어왔습니다. 모바일에서 한 열이라 일곱 번째 도구는 사실상 아무도 못 보는 상태였습니다. 두 열 + 콤팩트 카드로 바꿔 390×844에서 **7개 중 6개가 첫 화면에 들어옵니다**(측정값). 카드 전체가 이미 링크라 "도구 열기" 줄은 모바일에서 감췄습니다.

  검색은 Bubblelab `_shared/search-rules.js`를 그대로 가져왔습니다. **서버도 색인 빌드도 없는 룰베이스**입니다 — 정확·앞부분·부분·초성(`ㅅㄷㄹ`)·자모(덜 친 글자)·한영 오타(`tkekfl`)·비슷한 말 일곱 규칙에 점수가 정해져 있고, 설명이 필요한 규칙(초성·자모·오타·비슷한 말)만 결과에 이름을 답니다. 색인은 도구·변환 쌍·가이드 16개를 빌드 시점에 페이지 안에 넣습니다(gzip 1.7KB — 가지러 가는 요청 한 번보다 쌉니다).

  Utilark에 맞춰 두 군데를 고쳤습니다. ① **키워드 필드 가중치를 제목의 "부분 일치"보다 낮춰**(0.7) `convert`가 변환 쌍 여섯 개를 이미지 변환기 위에 올리던 것을 바로잡았습니다. ② **띄어쓴 질의를 붙여서도 한 번 더 채점**합니다 — `png to jpg`와 `jpg to png`는 같은 세 낱말이라 낱말별 채점으로는 동점이고, 그러면 사전순으로 절반은 반대 방향이 1등이 됐습니다. 붙이면 슬러그와 정확히 일치해 갈립니다.

  **엔진만 가져오고 보이는 면은 새로 썼습니다.** 처음에는 빈 결과 문구가 버블랩과 한 단어 차이(`영문 폴더 이름`→`영문 주소`)였고 플레이스홀더도 `검색 — X · ㅅㄷㄹ · Y` 형식을 그대로 따라 했습니다. 두 사이트는 브랜드가 다르고 같은 사람이 둘 다 볼 수 있으므로, 보이는 층이 같으면 Utilark가 복제본으로 읽힙니다 — `utilark.com` 문제와 같은 실패 방식입니다. 문구를 Utilark 어투(설명하는 완결문)로 다시 쓰고, 규칙 이름도 범주명(`초성 일치`)이 아니라 한 일(`초성으로 찾음`)로 바꾸고, 테두리 배지를 카테고리 옆 인라인 텍스트로 내렸습니다. 이 원칙은 `AGENTS.md`에 규칙으로 적었습니다.

  플레이스홀더는 360px에서 잘리지 않도록 후보를 실측해 골랐습니다(국문 257px / 영문 265px, 가용 280px).

  스크립트가 살아 있을 때만 입력창을 띄우므로 번들이 깨져도 그리드는 그대로 보입니다. IME 조합 중 Enter는 첫 결과로 보내지 않습니다(한글 음절을 확정하는 키입니다). `npm test`가 색인 개수·주소 접두사·2열 규칙을 검사하고, 규칙 엔진은 `test/search-rules.test.mts`가 덮습니다.

- **유효 방문자 집계 (2026-08-13)** — Bubblelab `admin`이 "유효 방문자/전체 브라우저 병기"로 실질 트래픽을 보는 방식을 가져왔습니다. 유효 판정 규칙은 `_shared/engagement.js`와 같습니다 — **화면에 실제로 표시된 채 3초 이상 머물거나 누르기·입력·휠·터치·스크롤이 있으면** 비콘 한 번. 탭이 가려진 동안은 시계가 멈추므로 배경 탭에서 저절로 채워지지 않습니다.

  **식별자는 가져오지 않았습니다.** Bubblelab은 브라우저마다 영구 UUID 쿠키를 쓰는데, 이 저장소의 처리방침은 "날짜를 넘어 사용자를 연결하는 식별자를 저장하지 않는다"고 약속합니다. 그래서 페이지뷰가 이미 쓰는 **일별 단방향 값을 그대로** 키로 씁니다 — 유효 방문을 세느라 새로 저장되는 정보가 없습니다.

  DNT·GPC·`utilark_notrack`·봇 판정이 페이지뷰와 동일하게 적용됩니다(테스트로 고정). 관리자 화면에 오늘 유효 방문자·전체 DAU·유효 비율을 나란히 띄우고 일별 표에 열을 하나 더했습니다.

  **공개 푸터 숫자는 그대로 뒀습니다.** 이미 게시 중인 지표의 정의를 말없이 바꾸는 일이라 요청 범위 밖으로 판단했습니다(통합 테스트가 이 변경을 잡아냈습니다). 공개 API는 두 값을 함께 내려주므로, 바꾸기로 하면 표시만 바꾸면 됩니다.

- **텍스트 읽어주기 (2026-08-13)** — 브라우저 내장 `speechSynthesis`로 만들었습니다. Bubblelab `podcast`는 Gemini TTS + R2 + 작업 큐라 "서버가 필요해 제외" 분류에 해당하지만, `util/brief`가 쓰는 `_shared/tts.js`는 브라우저 전용이라 그쪽을 참조했습니다.

  그 파일이 문서화한 함정을 그대로 처리했습니다 — `getVoices()` 첫 호출이 빔(`voiceschanged`를 1초까지 대기), 크롬이 긴 발화를 ~15초에 끊음(문장 단위 큐), 크롬 큐가 멎음(8초마다 `resume()`), iOS는 제스처 안에서만 첫 재생(핸들러에서 `await` 안 함), 남은 발화가 `speak()`를 막음(항상 `cancel()` 먼저), 페이지를 떠나도 소리가 남음(`pagehide`·`visibilitychange`), `stop()`이 던지는 `interrupted`/`canceled`는 실패가 아님.

  **개인정보 설계가 핵심입니다.** 일부 브라우저는 고품질 목소리를 자사 서버에서 만들어, 그 목소리를 고르면 읽는 글이 제조사로 전송됩니다. 목소리마다 `기기 내장`/`네트워크`를 표시하고, 기기 내장을 먼저 정렬하고, 네트워크 목소리를 고르면 재생 전에 경고합니다. 처방침(영·국문)과 README에 이 예외를 명시했습니다 — Utilark로 가는 전송은 아니지만 전송은 전송입니다.

- **사진 PDF 만들기 · 사진 자르기 (2026-08-13)** — 도구 두 개를 추가해 색인 페이지가 4개 늘었습니다(사이트맵 38 → 42). 둘 다 `pdf-lib`·캔버스 등 이미 쓰던 재료만 씁니다.

  자르기의 선택 영역은 화면 픽셀이 아니라 **원본 이미지 픽셀**로 들고 있어 화면을 돌려도 유지되고, 비율 프리셋에 **증명사진 3:4(30×40mm)와 여권·신분증 3.5:4.5(35×45mm)**가 있어 `passport-pic` 마이그레이션의 밑작업이 됩니다. 처음에는 3.5:4.5 하나만 두고 "증명사진"이라 불렀는데, 이력서에 붙이는 증명사진은 30×40mm이고 35×45mm는 여권·주민등록증 규격이라 서로 다른 인쇄물입니다. 둘로 나누고 이름을 바로잡았습니다. 자동 테두리 인식(OpenCV.js 8MB)을 붙이는 대신 수동 자르기 + 사진 PDF 조합으로 문서 스캔의 실속을 가져오는 선택입니다.

  사진 PDF는 넣기 전에 크기를 줄입니다(기본 긴 변 1600px). 원본 해상도로 넣으면 메일로 못 보내는 PDF가 나옵니다. A4 옵션은 가로 사진을 가로 페이지에 놓습니다.

  **EXIF 회전이 세 도구 공통의 함정이었습니다** — `createImageBitmap(file)`을 옵션 없이 부르면 폰 사진이 누울 수 있고, 자르기에서는 사용자가 고르지 않은 영역이 잘립니다. 세 곳 모두 `{ imageOrientation: 'from-image' }`를 명시합니다.

- **이미지 변환 쌍별 페이지 분리 (2026-08-13)** — `png-to-jpg`·`jpg-to-png`·`jpg-to-webp`·`webp-to-jpg`·`png-to-webp`·`webp-to-png` 여섯 쌍을 만들어 색인 페이지가 12개 늘었습니다(사이트맵 26 → 38). `docs/seo-research.md`의 최대 발견이었고 독립 사례가 넷이었습니다.

  각 페이지는 원본 형식으로 파일 선택이 잠기고 대상 형식이 미리 선택되며, PNG 대상은 무손실이라 화질 슬라이더를 감춥니다. `image-converter`는 여섯 쌍을 모으는 허브로 남아 "이미지 변환" 상위 개념어를 계속 담당합니다.

  **중복 클러스터링이 최대 위험이었습니다** — 여섯 페이지가 형식 이름만 바꾼 같은 글이면 구글이 묶어서 하나만 노출하고 분리한 의미가 사라집니다(`docs/how-search-works.md`). 방향마다 실제로 다른 이야기를 씁니다(무엇을 잃는가·무엇이 커지는가·왜 이 방향으로 바꾸는가). 측정한 본문 자카드 유사도는 최대 0.20입니다.

  `npm test`가 쌍끼리의 상호 링크, 허브 양방향 링크, 서브도메인 대응, 제목 공식을 검사합니다. 예약해 둔 `{from}2{to}` 서브도메인 여섯 개도 실제 페이지로 연결했습니다.

- **가이드 섹션 신설 (2026-08-13)** — `/{lang}/guides/`와 가이드 3편(자소서 글자수·이미지 형식 선택·PDF 안전하게 합치기)을 영문·국문으로 만들었습니다. 영문 488~538단어, 국문 1050~1260자로 기존 도구 페이지(235단어)보다 두껍습니다. 도구 페이지는 가이드를 가리키고 가이드는 도구를 가리켜 양방향으로 이어집니다. `npm test`가 분량 하한(영문 450단어·국문 900자), 양방향 링크, 목록 페이지의 전체 링크를 검사합니다. 근거는 `docs/seo-research.md`의 FoxyUtils 조사 — 상위 사이트는 키워드 하나를 도구 페이지·헬프 문서·블로그 세 지면으로 덮습니다.

- **도구 URL에서 `tools/` 제거 (2026-08-13)** — `/ko/tools/merge-pdf/` → `/ko/merge-pdf/`. `seo-research.md`의 7번 발견이 "하려면 지금"이라고 했던 항목이고, 같은 이름의 `utilark.com`이 `/en/tools/typing-test` 구조를 써서 이틀 된 이쪽이 모방으로 읽히는 문제도 함께 없앴습니다. 옛 경로는 Worker가 301로 보냅니다.
- **이름 유지 결정 (2026-08-13)** — `utilark.com`("UtilArk — All-in-One Utilities")이 같은 이름으로 en·fr·de·ja 유틸리티 사이트를 운영합니다. 유지하기로 했습니다. 근거는 ① 한국어 지면이 저쪽에 없고 ② 도구가 안 겹칩니다(저쪽 base64·JSON·UUID·텍스트 diff·타이핑, 이쪽 PDF·이미지·글자수·사다리). 영문 브랜드 검색 `utilark`는 포기하고 도구 키워드로 갑니다. **상표 확인은 운영자 몫으로 남아 있습니다** — 제가 판단할 수 있는 사항이 아닙니다.
- **도구 페이지 상호 링크 (2026-08-13)** — 도구 페이지 하단에 다른 도구 그리드를 넣었습니다. 그 전에는 도구 페이지에서 다른 도구로 가는 링크가 하나도 없어 막다른 길이었습니다. Foxit이 모든 도구 페이지에 전체 도구 그리드를 까는 것을 보고 적용했고, 이는 `docs/seo-research.md`가 열어 뒀던 "쌍 페이지끼리 내부 링크를 거는지" 질문의 답이기도 합니다. `npm test`가 모든 도구 페이지의 상호 링크를 검사합니다.
- **제목 태그 공식 적용 (2026-08-13)** — `ToolCopy.titleTag`를 추가하고 도구 8개 지면(4도구 × 2언어)에 적용했습니다. `PDF 합치기 · Utilark` → `PDF 합치기 - 무료 온라인 PDF 병합 | Utilark`. 한국어 제목에는 동의어·띄어쓰기 변형(`글자수`/`글자 수`, `사다리타기`/`사다리 게임`)을 함께 넣었습니다. `npm test`가 브랜드 형식·`무료`/`Free` 포함·60자 상한을 검사하며, 길이는 `&amp;`를 디코딩한 뒤 잽니다.
- **서브도메인 배포 (2026-08-13)** — `*.utilark.app/*` 와일드카드 라우트가 배포됐습니다. 준비 두 가지(와일드카드 CNAME 프록시 레코드, 배포 토큰의 `Zone → Workers Routes → Edit`)는 끝났고 최초 1회로 종료입니다. 앞으로 도구를 추가할 때 대시보드 작업이 없습니다. 미지수였던 `admin.utilark.app` Custom Domain과의 겹침은 **문제 없었습니다** — Cloudflare가 와일드카드 라우트를 받아들였고, Custom Domain이 자기 호스트에서 우선합니다. 정책과 매핑은 `docs/subdomains.md`에 있습니다.
- **도메인 메일 스푸핑 방지 (2026-08-13)** — `@utilark.app`으로 메일을 보내지도 받지도 않으므로 SPF(`v=spf1 -all`), DMARC(`p=reject`), 빈 DKIM TXT 레코드를 넣어 도용을 차단했습니다. MX는 받을 메일이 없어 넣지 않았습니다. 나중에 이 도메인으로 메일을 보낼 일이 생기면 SPF의 `-all`을 먼저 고쳐야 하며, 안 고치면 발송분이 전부 거부됩니다.
- **운영자 표기** — 운영자가 사업자가 아님을 확인했습니다. 개인정보 처리방침과 소개 페이지는 "회사가 아니라 한 명의 독립 개발자가 운영"으로 표기하며, 사업자등록번호·통신판매업 신고번호를 추가하라는 이전 권고는 철회합니다. 광고 수익이 발생하더라도 개인 운영 사이트로서 현재 표기가 맞습니다.
- **로또 번호 생성기** — 마이그레이션 후보에서 제외했습니다. AdSense는 도박 및 관련 콘텐츠를 제한하므로, 승인 심사를 앞둔 시점에 굳이 위험을 만들 이유가 없습니다.
