{
  "name": "utilark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-pdf-assets.mjs && astro dev",
    "start": "astro dev",
    "build": "astro build",
    "deploy": "npm run build && wrangler deploy",
    "preview": "astro preview",
    "check": "astro check",
    "test": "npm run check && npm run build && node scripts/check-built-seo.mjs && node --test worker/*.test.mjs test/*.test.mts",
    "prebuild": "node scripts/sync-pdf-assets.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@breezystack/lamejs": "^1.2.7",
    "astro": "^7.2.1",
    "gifenc": "^1.0.3",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.2.108"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "miniflare": "4.20260710.0",
    "typescript": "^6.0.3",
    "wrangler": "4.111.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
